Description: 'The rule detects suspicious command execution and scripting activity based on Windows security events'
EventDescriptions:
    - LocalizationId: 'corrname_correlation_41'
      EventDescription: 'User {subject.account.name} executed {subject.process.name} with command {subject.process.cmdline} on host {event_src.hostname}'
