Description: 'The rule detects authentication using valid accounts activity based on Windows security events'
EventDescriptions:
    - LocalizationId: 'corrname_correlation_20'
      EventDescription: 'User {subject.account.name} executed {subject.process.name} with command {subject.process.cmdline} on host {event_src.hostname}'
